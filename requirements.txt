# Dependencias fijadas con `pip freeze` sobre un entorno virtual limpio.
# Se fijan versiones exactas (no rangos) para builds reproducibles, tal
# como se instalan en el Dockerfile de produccion.

# --- Pipeline de datos / entrenamiento (seccion 1) ---
joblib==1.6.0
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.1
scipy==1.18.1

# --- Backend Flask (seccion 2) ---
Flask==3.1.3
Flask-Limiter==4.1.1
flasgger==0.9.7.1
gunicorn==26.2.0
psycopg2-binary==2.9.13
python-dotenv==1.2.3
tenacity==9.1.4
